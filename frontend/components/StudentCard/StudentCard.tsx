import React from "react";
import {Student} from "../../types/types";

export const StudentCard: React.FC<{ student: Student | undefined}> = ({student}) => {
    console.log(student)
    return (
        <div>

        </div>
    )
}
